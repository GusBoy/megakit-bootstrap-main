# 🔍 ДИАГНОСТИКА: Пропали секции

## ❗ Проблема
После увеличения логотипа пропали вторая и третья секции.

---

## 🎯 Быстрая диагностика

### Шаг 1: Откройте DEBUG страницу

```
theme/DEBUG_SECTIONS.html
```

Эта страница покажет ВСЕ секции разными цветами:
- 🟣 Секция 1 (Home) - фиолетовый
- 🔴 Секция 2 (Despre) - розовый
- 🔵 Секция 3 (Servicii) - синий
- 🟢 Секция 4 (Geismar) - зелёный
- 🟠 Секция 5 (Contact) - оранжевый

**Что проверить:**
```
□ Видны ВСЕ секции?
□ При клике на ссылки прокрутка работает?
□ Секции НЕ скрываются под меню?
```

---

### Шаг 2: Проверьте основной сайт

Откройте `theme/index.html` и **жёстко обновите**:
```
Ctrl + F5
```

**Проверьте:**
```
□ Секция "Despre" (3 карточки) видна?
□ Секция "Servicii" (5 карточек) видна?
□ При прокрутке секции появляются?
□ Они НЕ скрыты под белым меню?
```

---

### Шаг 3: Откройте консоль браузера

Нажмите **F12** и проверьте:

```javascript
// В консоли введите:
document.querySelector('#despre')
// Должно быть: <section id="despre"...>

document.querySelector('#servicii')
// Должно быть: <section id="servicii"...>

// Проверить видимость:
document.querySelector('#despre').getBoundingClientRect()
// Смотрите на "top" - должно быть > 0
```

---

## 🔧 Возможные причины

### Причина 1: Секции скрыты под fixed меню

**Симптомы:**
- Секции существуют в HTML
- Но при прокрутке "проскакивают"
- Верхняя часть скрыта под меню

**Решение:**
Увеличьте `padding-top` для `.slider` в `sticky-nav.css`:

```css
.slider {
  padding-top: 380px !important;  /* Увеличьте ещё на 20px */
}
```

---

### Причина 2: CSS не загружен

**Симптомы:**
- Логотип маленький (не увеличенный)
- Меню тёмное (не белое)
- Секции на месте

**Решение:**
```
1. Проверьте что файлы существуют:
   theme/css/sticky-nav.css ✅
   theme/css/logo-colors.css ✅

2. Очистите кэш:
   Ctrl + F5

3. Проверьте в index.html:
   <link rel="stylesheet" href="css/sticky-nav.css">
   <link rel="stylesheet" href="css/logo-colors.css">
```

---

### Причина 3: JavaScript ошибки

**Симптомы:**
- Секции не анимируются
- Консоль показывает ошибки
- Прокрутка не работает

**Решение:**
```
1. F12 → Console
2. Ищите ошибки (красный текст)
3. Проверьте что jQuery загружен:
   В консоли: typeof jQuery
   Должно быть: "function"
```

---

### Причина 4: Scroll-margin слишком большой

**Симптомы:**
- При клике на "Despre" прокручивает слишком далеко
- Пропускает секции

**Решение:**
Уменьшите `scroll-margin-top` в `sticky-nav.css`:

```css
#despre, #servicii, #geismar, #contact {
  scroll-margin-top: 140px;  /* Было 160px */
}
```

---

## 📊 Проверочная таблица

| Проверка | Как проверить | Ожидаемый результат |
|----------|---------------|---------------------|
| Секции в HTML | F12 → Elements | 5 секций с id |
| CSS загружен | F12 → Network → CSS | 200 OK |
| Логотип увеличен | Визуально | ~55px высота |
| Меню белое | Визуально | background: white |
| Despre видна | Прокрутить вниз | 3 карточки видны |
| Servicii видна | Прокрутить ещё | 5 карточек видны |

---

## 🛠️ Быстрое исправление

### Вариант 1: Увеличить отступ (рекомендуется)

**Файл:** `theme/css/sticky-nav.css`

```css
/* Найдите: */
.slider {
  padding-top: 360px !important;
}

/* Замените на: */
.slider {
  padding-top: 400px !important;  /* +40px */
}
```

---

### Вариант 2: Уменьшить логотип (не рекомендуется)

**Файл:** `theme/css/sticky-nav.css`

```css
/* Найдите: */
.navbar-brand .logo-img {
  height: 55px;
}

/* Замените на: */
.navbar-brand .logo-img {
  height: 45px;  /* Меньше */
}
```

---

### Вариант 3: Убрать fixed меню (временно для теста)

**Файл:** `theme/css/sticky-nav.css`

```css
/* Закомментируйте: */
/*
.navigation {
  position: fixed;
  ...
}
*/

/* И добавьте: */
.navigation {
  position: relative;
}
```

**Если секции появились - значит проблема в fixed меню!**

---

## 🔍 Детальная диагностика

### В консоли (F12) выполните:

```javascript
// 1. Проверить все секции
console.log('Секции на странице:');
['home', 'despre', 'servicii', 'geismar', 'contact'].forEach(id => {
  const el = document.querySelector(`#${id}`);
  console.log(`#${id}:`, el ? '✅ Есть' : '❌ НЕТ');
});

// 2. Проверить позиции секций
document.querySelectorAll('section').forEach(section => {
  const rect = section.getBoundingClientRect();
  console.log(section.id, {
    top: rect.top,
    visible: rect.top < window.innerHeight && rect.bottom > 0
  });
});

// 3. Проверить высоту меню
const navbar = document.querySelector('#navbar');
console.log('Высота navbar:', navbar ? navbar.offsetHeight + 'px' : 'НЕТ');

// 4. Проверить padding первой секции
const slider = document.querySelector('.slider');
const styles = window.getComputedStyle(slider);
console.log('Padding-top slider:', styles.paddingTop);
```

---

## ✅ После исправления проверьте:

```
□ Все 5 секций видны
□ При прокрутке секции не "прыгают"
□ Секции не скрыты под меню
□ Логотип увеличенный
□ Меню белое
□ Анимации работают
□ Мобильная версия ОК
```

---

## 📞 Если ничего не помогло

### Напишите мне в консоль что видите:

```javascript
// Скопируйте это в консоль и отправьте результат:
{
  sections: Array.from(document.querySelectorAll('section')).map(s => s.id),
  navbarHeight: document.querySelector('#navbar')?.offsetHeight,
  sliderPadding: window.getComputedStyle(document.querySelector('.slider')).paddingTop,
  cssFiles: Array.from(document.querySelectorAll('link[rel="stylesheet"]')).map(l => l.href)
}
```

Это покажет мне:
- Какие секции есть на странице
- Высоту меню
- Отступ slider
- Какие CSS загружены

---

## 🎯 Ожидаемый результат

После исправления вы должны видеть:

```
1️⃣ Секция Home (Hero) - на весь экран
   ↓ Прокрутить
2️⃣ Секция Despre - 3 карточки
   ↓ Прокрутить
3️⃣ Секция Servicii - 5 карточек
   ↓ Прокрутить
4️⃣ Секция Geismar - текст справа, картинка слева
   ↓ Прокрутить
5️⃣ Секция Contact - форма и контакты
```

**Все секции полностью видны, не скрыты под меню!**

---

## 🚀 Проверка работает!

Когда всё исправлено, откройте:
```
theme/index.html
```

И проверьте:
- ✅ Ctrl + F5 (жёсткое обновление)
- ✅ Прокрутите весь сайт вниз
- ✅ Все секции видны
- ✅ Логотип увеличен
- ✅ Меню белое
- ✅ Анимации работают

---

**Удачи с исправлением! 🎨**

© 2025 Mandril S.R.L
