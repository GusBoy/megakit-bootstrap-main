# 🐛 DEBUG: Анимация работает только при перезагрузке

## Проблема:
- ✅ При перезагрузке (F5) - анимация работает
- ❌ При прокрутке (scroll) - ничего не происходит

---

## ✅ Что я добавил:

### ПОДРОБНЫЕ ЛОГИ в консоль:

Теперь **каждое действие** выводится в консоль!

```javascript
📦 Script.js начал загрузку...
✅ Scroll event зарегистрирован!
🚀 Document Ready!
🔍 checkAnimations() вызвана! Раз: 1
📊 Найдено элементов для анимации:
   - Geismar: 1
   - Fade-in-up: 2
   - Scale-in: 8
🧪 Попробуйте прокрутить страницу вниз...
```

**При прокрутке:**
```javascript
📜 Scroll event #1
🔍 checkAnimations() вызвана! Раз: 2
   Высота окна: 969 Прокрутка: 145
   Элемент: section intro fade-in-up... Top: 1234 Visible: 1084
📜 Scroll event #31
🔍 checkAnimations() вызвана! Раз: 32
✅ ЭЛЕМЕНТ АНИМИРОВАН!
✅ GEISMAR АНИМИРОВАН!
```

---

## 🧪 ПРОВЕРКА:

### Шаг 1: Откройте страницу
```sh
cd megakit-bootstrap-main\theme
python -m http.server 8000
# Откройте: http://localhost:8000
```

### Шаг 2: Откройте консоль (F12)
**Должны быть сообщения:**
```
📦 Script.js начал загрузку...
✅ Scroll event зарегистрирован!
🚀 Document Ready!
```

### Шаг 3: Очистите кэш
```
Ctrl + F5 (жёсткая перезагрузка)
```

### Шаг 4: Прокрутите страницу вниз

**Должны появиться сообщения:**
```
📜 Scroll event #1
🔍 checkAnimations() вызвана! Раз: 2
```

---

## 🔍 ЧТО ПРОВЕРИТЬ:

### ✅ Если видите сообщения при прокрутке:
**Значит scroll работает!** Анимация должна запускаться.

### ❌ Если НЕ видите сообщения при прокрутке:
**Проблема в регистрации scroll event!**

Попробуйте:

#### 1. Проверьте конфликты с другими скриптами:
```javascript
// В консоли:
$(window).off('scroll'); // Отключить все scroll
$(window).on('scroll', function() {
  console.log('TEST SCROLL!');
});
```

Прокрутите - должно появиться `TEST SCROLL!`

#### 2. Проверьте что jQuery работает:
```javascript
// В консоли:
typeof jQuery
// Должно быть: "function"

jQuery.fn.jquery
// Версия jQuery, например: "3.6.0"
```

#### 3. Проверьте порядок загрузки скриптов:

В `index.html` должен быть **такой порядок**:
```html
<!-- Сначала jQuery -->
<script src="plugins/jquery/jquery.min.js"></script>

<!-- Потом остальные -->
<script src="plugins/bootstrap/bootstrap.min.js"></script>
<script src="plugins/slick/slick.min.js"></script>

<!-- В КОНЦЕ script.js -->
<script src="js/script.js"></script>
<script src="js/language.js"></script>
```

---

## 🐛 Возможные проблемы:

### Проблема 1: Конфликт с language.js

`language.js` может **перезаписывать** scroll event!

**Решение:** Проверьте `language.js`:
```javascript
// В language.js НЕ должно быть:
$(window).off('scroll'); // ← ПЛОХО!
$(window).on('scroll', function() { ... }); // ← Может конфликтовать
```

### Проблема 2: Smooth scroll перехватывает события

**Решение:** Отключите временно smooth scroll:
```javascript
// В script.js закомментируйте:
/*
$('.navbar-nav a[href^="#"]').on('click', function (event) {
  // ...
});
*/
```

### Проблема 3: CSS `scroll-behavior: smooth`

**Решение:** Отключите в style.css:
```css
html {
  /* scroll-behavior: smooth; */
}
```

---

## 📊 Таблица отладки:

| Сообщение в консоли | Что это значит | Статус |
|---------------------|----------------|--------|
| `📦 Script.js начал загрузку...` | Скрипт загружается | ✅ |
| `✅ Scroll event зарегистрирован!` | $(window).on('scroll') вызван | ✅ |
| `🚀 Document Ready!` | DOM готов | ✅ |
| `📜 Scroll event #1` | Прокрутка работает! | ✅ |
| `🔍 checkAnimations() вызвана!` | Функция запускается | ✅ |
| `✅ ЭЛЕМЕНТ АНИМИРОВАН!` | Анимация сработала | ✅ |

**Если НЕ видите `📜 Scroll event` - scroll не работает!**

---

## ✅ После исправления:

Вы должны увидеть в консоли:

```
📦 Script.js начал загрузку...
✅ Scroll event зарегистрирован!
🚀 Document Ready!
🔍 checkAnimations() вызвана! Раз: 1
📊 Найдено элементов:
   - Geismar: 1
   - Fade-in-up: 2
   - Scale-in: 8
🧪 Попробуйте прокрутить...
📜 Scroll event #1         ← При прокрутке!
🔍 checkAnimations() вызвана! Раз: 2
📜 Scroll event #31
🔍 checkAnimations() вызвана! Раз: 32
✅ ЭЛЕМЕНТ АНИМИРОВАН!     ← Анимация работает!
✅ GEISMAR АНИМИРОВАН!     ← Geismar работает!
```

---

## 🎯 Следующие шаги:

1. **Откройте страницу**
2. **F12** - откройте консоль
3. **Ctrl + F5** - очистите кэш
4. **Прокрутите вниз**
5. **Смотрите в консоль**

**Напишите мне что вы видите в консоли!**

---

Пример:
- "Вижу только `📦 Script.js начал загрузку...`"
- "Вижу `🚀 Document Ready!` но нет `📜 Scroll event`"
- "Вижу `📜 Scroll event` но нет `✅ АНИМИРОВАН`"

**Это поможет найти проблему!** 🔍
