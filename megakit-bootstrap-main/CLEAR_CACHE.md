# 🔄 Очистка кэша для исправления скачков картинки

## Проблема
После исправления файла `language.js` картинка всё ещё прыгает при переключении языков.

## Причина
Браузер использует **старую версию** файла из кэша.

---

## ✅ Решение 1: Жёсткая перезагрузка

### В Chrome/Edge:
```
Ctrl + Shift + R
```
или
```
Ctrl + F5
```

### В Firefox:
```
Ctrl + Shift + R
```

### В Safari (Mac):
```
Cmd + Shift + R
```

---

## ✅ Решение 2: Очистка кэша через DevTools

1. **Откройте DevTools**: `F12`
2. **Кликните правой кнопкой** на кнопке обновления в браузере
3. Выберите **"Empty Cache and Hard Reload"** (Очистить кэш и жёстко перезагрузить)

---

## ✅ Решение 3: Полная очистка кэша

### Chrome/Edge:
1. `Ctrl + Shift + Delete`
2. Выберите **"Кэшированные изображения и файлы"**
3. Период: **"Всё время"**
4. Нажмите **"Удалить данные"**

### Firefox:
1. `Ctrl + Shift + Delete`
2. Выберите **"Кэш"**
3. Период: **"Всё"**
4. Нажмите **"Удалить сейчас"**

---

## ✅ Решение 4: Режим инкогнито

Откройте сайт в режиме инкогнито для проверки:
```
Ctrl + Shift + N  (Chrome/Edge)
Ctrl + Shift + P  (Firefox)
```

Если в инкогнито всё работает - значит проблема была в кэше!

---

## 🧪 Проверка

После очистки кэша:

1. Откройте `index.html`
2. Откройте **DevTools** (`F12`)
3. Перейдите на вкладку **Console**
4. Переключите язык (RO → EN)
5. Проверьте:
   - ✅ Должно быть: `🌍 Переключение языка на: en`
   - ✅ Должно быть: `✅ Язык переключен на: en`
   - ❌ **НЕ должно быть** других логов

### Картинка НЕ должна прыгать:
- ✅ Остаётся на месте
- ✅ Плавно
- ✅ Без скачков

---

## 🔍 Если проблема не решена

### Проверьте версию файла:

1. **Откройте DevTools** (`F12`)
2. Перейдите на вкладку **Sources**
3. Найдите `js/language.js`
4. Проверьте содержимое:
   - ✅ Функция `changeLanguage()` должна заканчиваться на `console.log('✅ Язык переключен на:', lang);` и закрывающую скобку `}`
   - ❌ **НЕ должно быть** функций `updateActiveNavOnScroll()` или `throttle()`

### Если старая версия всё ещё загружается:

1. Закройте **ВСЕ окна** браузера
2. Откройте заново
3. Нажмите `Ctrl + Shift + R`

---

## 💡 Для разработки (чтобы избежать проблем с кэшем)

### Добавьте версию к файлу:

```html
<!-- В index.html измените: -->
<script src="js/language.js"></script>

<!-- На: -->
<script src="js/language.js?v=2"></script>
```

При следующем изменении меняйте `?v=2` на `?v=3` и т.д.

---

## ✅ Чек-лист проверки:

```
□ Нажал Ctrl + Shift + R
□ Открыл DevTools (F12)
□ Проверил Console - нет лишних логов
□ Проверил Sources - правильная версия файла
□ Переключил язык RO → EN
□ Картинка НЕ прыгает
□ Переключил язык EN → RO
□ Картинка НЕ прыгает
□ Проверил в режиме инкогнито
□ Работает идеально!
```

---

**Статус:** ✅ ГОТОВО  
**Файл исправлен:** `language.js`  
**Осталось:** Очистить кэш браузера
