# 🔍 ДИАГНОСТИКА: Логотип не видно

## ❗ Проблема
Логотип не отображается на сайте после изменений.

---

## 🧪 БЫСТРАЯ ПРОВЕРКА

### Шаг 1: Откройте тестовую страницу
```
theme/TEST_LOGO.html
```

**Что должно быть:**
- 5 тестов с логотипом на разных фонах и размерах
- Логотип должен быть ВИДЕН во всех тестах
- Буквы M, NDRIL - темно-синие
- Буква 'a' - оранжевая внутри рамки

**Если логотип ВИДЕН на тестовой странице:**
✅ Файл SVG в порядке, проблема в CSS основного сайта

**Если логотип НЕ ВИДЕН:**
❌ Проблема с файлом SVG или путями

---

## 🔧 ИСПРАВЛЕНИЯ

### Исправление 1: Обновил SVG файл
**Что сделано:**
- ❌ Убрал зависимость от Google Fonts (могли не загрузиться)
- ✅ Использую системный шрифт Arial Black
- ✅ Добавил явные inline стили с цветами

**Файл:** `theme/images/logo-new.svg`

---

### Исправление 2: Обновил CSS
**Что сделано:**
- ✅ Добавил `opacity: 1 !important`
- ✅ Добавил `visibility: visible !important`
- ✅ Добавил `display: block !important`
- ✅ Убрал все фильтры `filter: none !important`

**Файл:** `theme/css/sticky-nav.css`

---

## 📊 ПРОВЕРЬТЕ В КОНСОЛИ БРАУЗЕРА

### Откройте основной сайт (index.html)

Нажмите **F12** и введите в Console:

```javascript
// 1. Проверить существование элемента логотипа
const logo = document.querySelector('.logo-img');
console.log('Логотип найден:', logo);

// 2. Проверить видимость
if (logo) {
    const styles = window.getComputedStyle(logo);
    console.log('Display:', styles.display);
    console.log('Visibility:', styles.visibility);
    console.log('Opacity:', styles.opacity);
    console.log('Filter:', styles.filter);
    console.log('Height:', styles.height);
}

// 3. Проверить загрузку SVG
const img = document.querySelector('.logo-img');
if (img) {
    console.log('Src:', img.src);
    console.log('Complete:', img.complete);
    console.log('Natural Height:', img.naturalHeight);
}
```

**Ожидаемые результаты:**
```
Логотип найден: <img class="logo-img" ...>
Display: block
Visibility: visible
Opacity: 1
Filter: none
Height: 55px
Src: http://localhost:8000/images/logo-new.svg
Complete: true
Natural Height: > 0
```

---

## 🎯 ВОЗМОЖНЫЕ ПРИЧИНЫ

### Причина 1: SVG файл не загружается
**Симптомы:**
- В консоли ошибка 404
- `img.complete = false`
- `img.naturalHeight = 0`

**Решение:**
1. Проверьте путь к файлу
2. Убедитесь что файл существует: `theme/images/logo-new.svg`
3. Откройте файл напрямую в браузере: `http://localhost:8000/images/logo-new.svg`

---

### Причина 2: CSS скрывает логотип
**Симптомы:**
- Логотип в DOM есть
- `display: none` или `visibility: hidden` или `opacity: 0`

**Решение:**
Уже исправлено в `sticky-nav.css`:
```css
.navbar-brand .logo-img {
  display: block !important;
  visibility: visible !important;
  opacity: 1 !important;
}
```

---

### Причина 3: Текст в SVG не рендерится
**Симптомы:**
- SVG загружается
- Но содержимое пустое
- Шрифт не найден

**Решение:**
Уже исправлено - заменил Google Fonts на Arial Black (системный шрифт)

---

### Причина 4: Белый логотип на белом фоне
**Симптомы:**
- Логотип есть, но не виден
- На темном фоне виден, на белом нет

**Решение:**
Проверьте цвета в SVG:
```xml
style="fill: #0d4a6d;"  ← Темно-синий, виден на белом
style="fill: #c97234;"  ← Оранжевый, виден на белом
```

---

## 🔄 ПЛАН ДЕЙСТВИЙ

### План А: Если тест-страница работает
1. ✅ SVG файл в порядке
2. Проверить CSS на основном сайте
3. Проверить конфликты стилей
4. Проверить порядок загрузки CSS файлов

---

### План Б: Если тест-страница НЕ работает
1. ❌ Проблема с SVG файлом
2. Открыть SVG в текстовом редакторе
3. Проверить синтаксис XML
4. Создать резервную PNG версию

---

## 🛠️ РЕЗЕРВНОЕ РЕШЕНИЕ

### Если SVG не работает - используем PNG

Создайте PNG версию логотипа и замените в HTML:

```html
<!-- Вместо SVG -->
<img src="images/logo-new.svg" alt="Mandril S.R.L" class="logo-img">

<!-- Используйте PNG -->
<img src="images/logo.png" alt="Mandril S.R.L" class="logo-img">
```

---

## ✅ ПРОВЕРОЧНЫЙ СПИСОК

После всех исправлений проверьте:

```
□ TEST_LOGO.html - логотип виден во всех 5 тестах
□ index.html - логотип виден в navbar
□ index.html - логотип виден в footer
□ Логотип цветной (не черно-белый)
□ Темно-синие буквы M, NDRIL
□ Оранжевая буква 'a'
□ Темно-синяя рамка вокруг 'a'
□ Размер 55px в navbar
□ Размер 60px в footer
□ Hover эффект работает (увеличение при наведении)
□ Нет ошибок в консоли (F12)
□ SVG загружается (Network tab в DevTools)
```

---

## 📞 ЕСЛИ НИЧЕГО НЕ ПОМОГЛО

### Скопируйте это в консоль и отправьте результат:

```javascript
{
  logoElement: document.querySelector('.logo-img') ? 'ЕСТЬ' : 'НЕТ',
  logoSrc: document.querySelector('.logo-img')?.src,
  logoComplete: document.querySelector('.logo-img')?.complete,
  logoNaturalHeight: document.querySelector('.logo-img')?.naturalHeight,
  computedStyles: window.getComputedStyle(document.querySelector('.logo-img') || document.body),
  svgFileExists: await fetch('images/logo-new.svg').then(r => r.ok).catch(() => false)
}
```

Это покажет мне:
- Есть ли элемент логотипа в DOM
- Какой путь к файлу
- Загрузился ли файл
- Какие стили применены
- Существует ли SVG файл

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После всех исправлений:

- ✅ Логотип виден на TEST_LOGO.html
- ✅ Логотип виден на index.html
- ✅ Цветной (темно-синий + оранжевый)
- ✅ Увеличенный (55px в navbar)
- ✅ Hover эффект работает

---

## 🚀 БЫСТРЫЕ КОМАНДЫ

### Открыть тест:
```
theme/TEST_LOGO.html
```

### Открыть основной сайт:
```
theme/index.html
Ctrl + Shift + F5
```

### Открыть SVG напрямую:
```
theme/images/logo-new.svg
```

---

**Проверьте TEST_LOGO.html и напишите результат!** 🔍

© 2025 Mandril S.R.L
