# ✨ Современные эффекты и анимации

## 🎨 Что было добавлено

Вдохновившись сайтом https://axdeninternational.com/, мы добавили следующие современные эффекты:

---

## 📜 Анимации при прокрутке (Scroll Animations)

### 1. **Fade In Up** - Появление снизу вверх
- Применяется к: заголовкам секций, карточкам услуг
- Эффект: элементы плавно появляются снизу с затуханием

### 2. **Scale In** - Масштабирование
- Применяется к: карточкам "intro-item", CTA блокам
- Эффект: элементы увеличиваются от 90% до 100%

### 3. **Slide In** - Скольжение
- Применяется к: секции "О компании"
- Эффект: элементы въезжают слева или справа

### 4. **Fade In** - Плавное появление
- Применяется к: общим блокам
- Эффект: простое появление с затуханием

---

## 🎭 Hover эффекты (При наведении мыши)

### 1. **Карточки услуг** (.service-item)
```
При наведении:
✓ Подъем карточки на 10px вверх
✓ Появление тени (shadow)
✓ Белый фон
✓ Иконка увеличивается и меняет цвет
✓ Иконка поворачивается на 5°
```

### 2. **Intro карточки** (.intro-item)
```
При наведении:
✓ Подъем на 10px
✓ Тень снизу
✓ Иконка "пульсирует"
✓ Белый фон появляется
```

### 3. **Кнопки** (.btn)
```
При наведении:
✓ Подъем на 3px
✓ Цветная тень
✓ Стрелка двигается вправо
✓ Волновой эффект (overlay)
```

### 4. **Навигационное меню**
```
При наведении:
✓ Линия под ссылкой появляется
✓ Цвет меняется на красный
✓ Плавный переход
```

---

## 🌊 Parallax эффект

### Что это?
Фон движется медленнее чем контент при прокрутке

### Где применяется:
- 🏠 Главная секция (Hero)
- 📞 Секция Contact (CTA)
- 📊 Секция Counter

### Как работает:
```javascript
scrolled = window scroll position
background moves at 50% speed of scroll
```

---

## 🔄 Анимация иконок

### 1. **Pulse** - Пульсация
```css
Иконки "дышат":
100% → 105% → 100% (размер)
Бесконечный цикл
```

### 2. **Float** - Плавание
```css
Иконки плавно двигаются вверх-вниз:
0px → -10px → 0px
Создает ощущение легкости
```

### 3. **Rotate on Hover** - Поворот
```css
При наведении иконка:
- Увеличивается на 20%
- Поворачивается на 5°
- Меняет цвет на красный
```

---

## 🎯 Эффекты прокрутки

### 1. **Навбар с тенью**
```
При прокрутке > 50px:
✓ Добавляется более сильная тень
✓ Создает эффект "прилипания"
```

### 2. **Появление элементов**
```
Элементы появляются когда:
- Попадают в область видимости
- Каждый со своей задержкой (stagger effect)
- Один раз (не повторяется)
```

### 3. **Scroll to Top кнопка**
```
При прокрутке > 200px:
✓ Кнопка появляется справа внизу
✓ При hover - подъем и тень
✓ Плавная прокрутка наверх
```

---

## 🎬 Задержки анимаций (Stagger Effect)

### Карточки появляются по очереди:
```
.delay-1 { animation-delay: 0.1s; }
.delay-2 { animation-delay: 0.2s; }
.delay-3 { animation-delay: 0.3s; }
.delay-4 { animation-delay: 0.4s; }
.delay-5 { animation-delay: 0.5s; }
```

### Где используется:
- 🎯 3 карточки Intro (Piețe, Calitate, Expertiză)
- 🔧 5 карточек Servicii
- 👥 Элементы Footer

---

## 💫 Переходы и Трансформации

### Все интерактивные элементы имеют:
```css
transition: all 0.3s ease;
```

### Типы трансформаций:
1. **translateY()** - движение вверх/вниз
2. **translateX()** - движение влево/вправо
3. **scale()** - изменение размера
4. **rotate()** - поворот

---

## 🎨 Тени и свечение

### 1. **Мягкие тени** (Soft shadows)
```css
box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
```

### 2. **Цветные тени** (Colored shadows)
```css
box-shadow: 0 10px 25px rgba(247, 87, 87, 0.3);
```

### 3. **Свечение у кнопок языка**
```css
box-shadow: 0 5px 15px rgba(247, 87, 87, 0.3);
```

---

## 🔥 Специальные эффекты

### 1. **Wave Effect на кнопках**
```
При клике белая волна проходит через кнопку
Создается с помощью ::before pseudo-element
```

### 2. **Underline Animation**
```
При hover под ссылками появляется линия
Растет от центра к краям
```

### 3. **Card Lift Effect**
```
Карточки "поднимаются" при hover
Создает эффект глубины
```

---

## 📱 Адаптивность

### Все эффекты работают на:
- ✅ Desktop (полные эффекты)
- ✅ Tablet (упрощенные анимации)
- ✅ Mobile (базовые transitions)

### На мобильных отключены:
- ❌ Parallax (для производительности)
- ❌ Сложные hover эффекты
- ✅ Остаются базовые transitions

---

## 🚀 Производительность

### Оптимизация:
1. **CSS анимации** вместо JavaScript (быстрее)
2. **transform** вместо position (GPU ускорение)
3. **will-change** для критичных элементов
4. **Debouncing** для scroll events

### Результат:
- ⚡ Плавные 60 FPS
- 📉 Низкая нагрузка на процессор
- 🎯 Быстрая отрисовка

---

## 🎓 Как использовать

### 1. Добавить анимацию к элементу:
```html
<div class="my-element fade-in-up">Content</div>
```

### 2. С задержкой:
```html
<div class="my-element scale-in delay-2">Content</div>
```

### 3. Создать свой эффект:
```css
.my-animation {
  opacity: 0;
  animation: myEffect 0.8s ease forwards;
}

@keyframes myEffect {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

---

## 🔧 Настройка

### Изменить скорость анимаций:
В `style.css` найдите:
```css
transition: all 0.3s ease;
```
Измените `0.3s` на другое значение (например, `0.5s` для медленнее)

### Изменить delay:
```css
.delay-1 { animation-delay: 0.2s; } /* было 0.1s */
```

### Отключить анимации:
Закомментируйте в `script.js`:
```javascript
// animateOnScroll(); // Отключено
```

---

## 🎯 Сравнение с Axden

| Эффект | Axden | Mandril | Статус |
|--------|-------|---------|--------|
| Scroll Animations | ✅ | ✅ | Реализовано |
| Parallax | ✅ | ✅ | Реализовано |
| Hover Effects | ✅ | ✅ | Реализовано |
| Smooth Scroll | ✅ | ✅ | Реализовано |
| Card Animations | ✅ | ✅ | Улучшено |
| Icon Effects | ✅ | ✅ | Добавлено |
| Navbar Shadow | ✅ | ✅ | Реализовано |
| Stagger Effect | ✅ | ✅ | Реализовано |

---

## 📊 Список всех анимаций

### CSS Keyframes:
1. ✨ `fadeInUp` - появление снизу
2. ✨ `fadeIn` - плавное появление
3. ✨ `slideInLeft` - въезд слева
4. ✨ `slideInRight` - въезд справа
5. ✨ `scaleIn` - масштабирование
6. ✨ `pulse` - пульсация
7. ✨ `float` - плавание

### JavaScript Effects:
1. 🔄 Scroll animations trigger
2. 🔄 Parallax background
3. 🔄 Navbar shadow on scroll
4. 🔄 Smooth scroll to anchors
5. 🔄 Scroll to top button

---

## 🎨 Цветовая палитра эффектов

### Основные цвета:
- 🔴 Accent: `rgb(247, 87, 87)` - красный Mandril
- ⚫ Dark: `#242424` - темный текст
- ⚪ Light: `#f5f8f9` - светлый фон
- 🌫️ Shadow: `rgba(0, 0, 0, 0.1)` - мягкие тени
- 🔥 Glow: `rgba(247, 87, 87, 0.3)` - красное свечение

---

## ✅ Что проверить

После добавления эффектов:
- [ ] Анимации работают плавно (60 FPS)
- [ ] Нет "дерганий" при прокрутке
- [ ] Parallax работает на desktop
- [ ] Hover эффекты работают
- [ ] Кнопки анимируются
- [ ] Scroll to top появляется
- [ ] Навбар получает тень
- [ ] Карточки появляются по очереди
- [ ] Мобильная версия работает
- [ ] Нет ошибок в консоли

---

## 🚀 Следующие шаги

### Можно добавить:
1. ⚡ **Preloader** - анимация загрузки
2. 🎬 **Video background** - видео в hero секции
3. 🌈 **Gradient animations** - анимированные градиенты
4. ✨ **Particle effects** - частицы на фоне
5. 🎯 **Counter animations** - анимация цифр
6. 📸 **Image reveal** - постепенное появление фото
7. 🔄 **Page transitions** - переходы между страницами

---

## 📞 Помощь

Если эффекты не работают:

1. **Очистите кэш браузера** (Ctrl + F5)
2. **Проверьте консоль** на ошибки (F12)
3. **Убедитесь что jQuery загружен** перед script.js
4. **Проверьте порядок подключения** CSS и JS файлов

---

**Enjoy the smooth animations! ✨**

© 2025 Mandril S.R.L
