# 🛠️ Полезные команды и инструменты

## 🚀 Запуск локального сервера

### Windows

#### Двойной клик:
- Запустите файл `START_SERVER.bat`
- Откройте http://localhost:8000

#### Вручную через PowerShell:
```powershell
cd theme
python -m http.server 8000
```

### Mac/Linux:
```bash
cd theme
python3 -m http.server 8000
```

## 📝 Git команды

### Первая публикация
```bash
# Инициализация
git init
git add .
git commit -m "Initial commit: Mandril website"

# Подключение к GitHub
git remote add origin https://github.com/USERNAME/REPO.git
git branch -M main
git push -u origin main
```

### Обновление сайта
```bash
# Посмотреть изменения
git status

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

### Отмена изменений
```bash
# Отменить изменения в файле (до коммита)
git checkout -- filename.html

# Отменить последний коммит (сохранив изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалив изменения) ⚠️
git reset --hard HEAD~1
```

### Просмотр истории
```bash
# Посмотреть историю коммитов
git log

# Посмотреть краткую историю
git log --oneline

# Посмотреть изменения в файле
git diff filename.html
```

## 🌐 Открытие сайта в браузере

### Windows CMD:
```cmd
start http://localhost:8000
```

### PowerShell:
```powershell
Start-Process "http://localhost:8000"
```

### Mac:
```bash
open http://localhost:8000
```

### Linux:
```bash
xdg-open http://localhost:8000
```

## 🔍 Проверка сайта

### Открыть консоль разработчика
- Chrome/Edge: `F12` или `Ctrl + Shift + I`
- Firefox: `F12` или `Ctrl + Shift + K`
- Safari: `Cmd + Option + I`

### Режим мобильного устройства
- Chrome/Edge: `Ctrl + Shift + M`
- Firefox: `Ctrl + Shift + M`

### Очистка кэша браузера
- Жесткая перезагрузка: `Ctrl + F5`
- Очистка кэша: `Ctrl + Shift + Delete`

## 📦 Создание резервной копии

### ZIP архив (Windows):
```powershell
Compress-Archive -Path theme\* -DestinationPath backup_$(Get-Date -Format 'yyyy-MM-dd').zip
```

### ZIP архив (Mac/Linux):
```bash
zip -r backup_$(date +%Y-%m-%d).zip theme/
```

### Копирование папки:
```bash
# Windows
xcopy theme theme_backup /E /I

# Mac/Linux
cp -r theme theme_backup
```

## 🔧 Проверка зависимостей

### Проверить установку Python:
```bash
python --version
# или
python3 --version
```

### Проверить установку Git:
```bash
git --version
```

### Проверить установку Node.js (опционально):
```bash
node --version
npm --version
```

## 📊 Полезные расширения для VS Code

### Рекомендуемые:
1. **Live Server** - локальный сервер с автообновлением
2. **HTML CSS Support** - автодополнение CSS в HTML
3. **Auto Rename Tag** - автоматическое переименование парных тегов
4. **Prettier** - форматирование кода
5. **GitLens** - расширенная работа с Git

### Установка:
1. Откройте VS Code
2. Нажмите `Ctrl + Shift + X`
3. Найдите расширение
4. Нажмите "Install"

## 🎨 Быстрое редактирование

### Изменить цвет сайта:
1. Откройте `theme/css/style.css`
2. Найдите: `rgb(247, 87, 87)` (красный цвет)
3. Замените на свой цвет (например: `#1a4d7d`)
4. Замените ВСЕ вхождения (Ctrl + H → Replace All)

### Изменить тексты:
1. Откройте `theme/js/language.js`
2. Найдите нужный текст в секции `translations.ro` или `translations.en`
3. Измените текст
4. Сохраните файл

### Изменить изображения:
1. Замените файлы в папке `theme/images/`
2. Или измените пути в CSS файле

## 🧪 Тестирование

### Тест скорости сайта:
- https://pagespeed.web.dev/
- Вставьте URL вашего сайта
- Получите рекомендации

### Валидация HTML:
- https://validator.w3.org/
- Вставьте URL или код
- Исправьте ошибки

### Проверка адаптивности:
- http://responsivetesttool.com/
- Введите URL сайта
- Проверьте на разных устройствах

### Проверка ссылок:
- https://validator.w3.org/checklink
- Проверка битых ссылок

## 📱 Эмуляция мобильных устройств

### В Chrome DevTools:
1. Откройте DevTools (F12)
2. Нажмите `Ctrl + Shift + M`
3. Выберите устройство из списка
4. Или задайте кастомный размер

### Популярные разрешения:
- iPhone SE: 375 x 667
- iPhone 12: 390 x 844
- Samsung Galaxy: 360 x 740
- iPad: 768 x 1024
- Desktop: 1920 x 1080

## 🔐 Безопасность

### Проверка безопасности:
- https://observatory.mozilla.org/
- Вставьте URL сайта

### HTTPS проверка:
- https://www.ssllabs.com/ssltest/
- Проверка SSL сертификата

## 📈 SEO инструменты

### Google Search Console:
1. https://search.google.com/search-console
2. Добавьте ваш сайт
3. Проверьте индексацию

### Мета-теги:
- https://metatags.io/
- Проверьте, как выглядит сайт в соцсетях

## 💡 Полезные советы

### Перед каждым обновлением:
```bash
# 1. Проверьте статус
git status

# 2. Посмотрите изменения
git diff

# 3. Протестируйте локально
python -m http.server 8000

# 4. Только потом публикуйте
git add .
git commit -m "Update"
git push
```

### Автоматизация:
Создайте файл `update.bat`:
```batch
@echo off
git add .
git commit -m "Update: %date% %time%"
git push
echo Done!
pause
```

### Workflow:
1. ✏️ Редактируйте файлы
2. 💾 Сохраните изменения
3. 🧪 Проверьте локально
4. ✅ Убедитесь, что все работает
5. 📤 Загрузите на GitHub
6. 🌐 Проверьте опубликованный сайт

## 🆘 Получение помощи

### Официальная документация:
- [Git](https://git-scm.com/doc)
- [GitHub Pages](https://docs.github.com/pages)
- [Bootstrap](https://getbootstrap.com/docs/)
- [jQuery](https://api.jquery.com/)

### Сообщества:
- Stack Overflow: https://stackoverflow.com/
- GitHub Discussions
- Reddit: r/webdev

## 📞 Контакты

Для технической поддержки:
- Email: info@mandril.md
- GitHub Issues: https://github.com/USERNAME/REPO/issues

---

© 2025 Mandril S.R.L
